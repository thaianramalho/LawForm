<Window x:Class="LawForm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LawForm"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="DeVence LawForm" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Row for Button -->
            <RowDefinition Height="*"/>
            <!-- Row for rest of content -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <!-- Column for Button -->
            <ColumnDefinition Width="Auto"/>
            <!-- Column for Image -->
        </Grid.ColumnDefinitions>

        <Button x:Name="advogadosScreen" Content="Advogados" 
                Grid.Row="0" Grid.Column="0"
                HorizontalAlignment="Left" Margin="10" VerticalAlignment="Top" 
                Click="advogadosScreen_Click"/>

        <Image Grid.Row="1" Grid.Column="1"
               HorizontalAlignment="Right" VerticalAlignment="Bottom"
               Margin="10"
               Height="45" Width="67" Source="/img/logodv.png"/>
    </Grid>
</Window>
